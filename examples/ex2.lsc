let pi = 3.14159265358979312 in
let sin_osc = fun freq -> sin << (2. * pi * freq * t) in
let tri_osc = fun freq -> triangle << (2. * pi * freq * t) in
let sqr_osc = fun freq -> square << (2. * pi * freq * t) in
sin_osc (440.) + (0.1 * noise)
